import SingleCourseWrapper from "@/features/singlecourse/components/SingleCourseWrapper";

const SingleCoursePage = ({ id }: { id: string }) => {
    return <SingleCourseWrapper id={id} />;
};

export default SingleCoursePage;
